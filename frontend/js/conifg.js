// Frontend config: where your backend API lives.
// For local dev, leave as localhost:4000.
// For production, change to your deployed backend URL.

window.NEWS_THING_CONFIG = {
  apiBase: "http://localhost:4000"
};
